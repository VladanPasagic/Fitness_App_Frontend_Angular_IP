import { Injectable } from '@angular/core';
import { LoginRequest } from '../Types/login-request';

@Injectable({
  providedIn: 'root',
})
export class LoginService {
  constructor() {}

  public sendRequest(request: LoginRequest) {
    
  }
}
